# PeerCollab-Backend

Backend for PeerCollab built with Spring Boot 3, Spring Security, JPA, JWT cookie auth, WebSocket, and MySQL.

## Run

```bash
mvn clean install
mvn spring-boot:run
```

## Deploy

- Render: use render.yaml or Dockerfile
- Set SPRING_PROFILES_ACTIVE=prod and all required env vars
